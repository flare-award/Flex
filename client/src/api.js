// Deprecated: old REST API client removed for P2P Firebase version.
// This stub prevents import errors if old components still reference it.
export const api = {
  // no-op stub
};
export function getToken() { return null; }
export function setToken() {}
